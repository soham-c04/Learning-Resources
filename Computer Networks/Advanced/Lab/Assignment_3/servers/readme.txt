"server" is for assignment 3.1 and "server2" is for assignment 3.2.
You can see help by adding -h or --help flag with the executable.

Run server as - `./server -p 2000 -f ../../sintel-1024-surround.mp4 -d 30` (Add any other flags as per requirement)